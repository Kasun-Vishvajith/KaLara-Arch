# Starting and resuming with Codex

## Fresh project — paste this

~~~text
Build KaLara Arch from this workspace's specification pack. Read AGENTS.md,
KA_LARA_ARCH_SAFE_ENV.md, docs/01_PRODUCT_SCOPE.md, docs/17_EXECUTION_ROADMAP.md,
and docs/PROGRESS.md first. Implement steps 00–26 in order as a Windows
C++20/Qt 6 desktop application exclusively for 2D architectural drafting.
I authorize ordinary repository-local implementation, builds, tests, and focused
Git commits. Continue autonomously after passing steps without requiring
CONTINUE. Stop only for a real blocker or a material decision outside the pack.
Keep progress and evidence current for future sessions. Deliver working features,
not just scaffolding. No runtime AI and no 3D. SVG art may use the specified
procedural fallback. Never report unfinished features or unrun checks as passing.
Start with Step 00 and continue through the complete local 2D release gate.
~~~

## Existing repository — paste this

Keep the new pack beside the existing repository until adoption. Replace the bracketed path.

~~~text
Use the new KaLara_Arch_2D_Codex_Pack at [ABSOLUTE PACK PATH] to finish my existing
KaLara Arch repository. First read applicable repository instructions and inspect
the code, Git status, tests, and build. Read the pack's source reconciliation.
Create a reviewable adoption diff and baseline feature audit. I authorize adopting
this pack's strictly 2D local scope and autonomous workflow in place of conflicting
older 3D/AI milestones and the CONTINUE rule. Archive superseded planning files;
do not delete source or overwrite unrelated work. Reuse proven systems. Map each
new step to verified existing behavior or remaining work, then complete missing
steps 00–26 in dependency order. I authorize repository-local changes, builds,
tests, and focused commits. Keep progress/evidence current. No runtime AI, no 3D,
no fake passing checks, and no UI-only completion claims.
~~~

## Resume

~~~text
Continue KaLara Arch from docs/PROGRESS.md. Read AGENTS.md, the active step,
its linked contracts, and the latest evidence. Inspect actual working state.
Resume unfinished authorized work without restarting completed stages.
Fix mandatory failures and continue through the local 2D release gate.
~~~

## Fix an issue without losing the plan

~~~text
Remain on the current step and fix: [issue]. Reproduce it, identify the affected
contract, implement the smallest complete fix, and verify relevant regressions.
Update evidence before advancing. Do not weaken the specification to make it pass.
~~~

## Optional review mode

Add “Use review mode; pause after each passing step for my next instruction” if you want stage-by-stage approval. This changes pacing, not the completion criteria.

Keep all files in the repository rather than pasting the full pack into every prompt. Codex should read the active step and linked contracts. A long specification still requires sustained implementation and multiple sessions.

Use Windows for native acceptance. Other platforms can test portable core code, but native UI/packaging results remain NOT RUN until tested on Windows. Dependency versions are pinned from the actual compatible environment during Step 00.
